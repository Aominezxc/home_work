import { Address } from "../models/address.model";

// async function createAdress(request, reply) {
//   const createAdress = new Adress(request.body);
//   createAdress.save();
//   const newUser = User.
// }

// async function getUserWithAdress(request, reply) {
//   const res = await Adress.findById(request.params.id).populate("adress");
//   return res;
// }
module.exports = {
  //   createAdress,
  //   getUserWithAdress,
};
